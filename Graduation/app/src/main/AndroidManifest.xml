<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Graduation"
        tools:targetApi="31">

        <!--  注册 HomeActivity  -->
        <activity android:name=".B_tablayout"/>
        <activity android:name=".C_my"/>
        <activity android:name=".C_lol"/>
        <activity android:name=".C_tft"/>
        <activity android:name=".C_event"/>
        <activity android:name=".Z_Setup"/>

        <activity
            android:name=".A_Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>